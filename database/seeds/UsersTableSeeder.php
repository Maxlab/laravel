<?php

use Illuminate\Database\Seeder;

class UsersTableSeeder extends Seeder
{
    public function run()
    {
//        factory(App\Models\User::class, 10)->create()->each(function ($user) {
//            $galleries = factory(\App\Models\Gallery::class, 2)->create(['user_id' => $user->id]);
//            $photos = factory(\App\Models\Photo::class, 50)->create();
//            foreach ($photos as $photo) {
//                $photo->galleries()->attach($galleries->random());
//            }
//        });
    }
}
